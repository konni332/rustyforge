dependencies = []

[project]
name = "complex-project"
version = "0.1.0"

[project.profile_debug]
flags = ["-Wall", "-Wextra"]
defines = ["DEBUG=1"]

[project.profile_release]
flags = ["-O3"]
defines = ["NDEBUG=1"]

[build]
ignore_patterns = []
